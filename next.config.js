/** @type {import('next').NextConfig} */
const nextConfig = {
  // Config options here
  experimental: {
    // Enable if needed for 3D rendering
  },
  images: {
    domains: ['localhost'],
  },
}

module.exports = nextConfig